{
  "name": "unityrtc-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "cp ./common.types.ts ./server/source/ && cp ./common.types.ts ./client/source",
    "build": "npm run prebuild && cd server && npm run build && cd ../client && npm run build"
  },
  "author": "",
  "license": "ISC"
}
