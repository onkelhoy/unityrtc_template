{
  "name": "unityrtc-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd server && npm run build && cd ../client && npm run build && cd .. && node movePublic.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^9.1.0"
  }
}
